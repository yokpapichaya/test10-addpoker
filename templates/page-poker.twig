{% extends 'base.twig' %}
{% block content %}
<section class="sec-page-content" data-aos="fade-up" data-aos-duration="1000">
    <div class="container">
        {{ options_poker_content }}
    </div>
</section>

<section class="sec-blockslot">
    <div class="container">
        <div class="box-product-container">
            {% for box in options_poker_block %}
            {% set content = TimberPost(box.poker_block_link) %}
            <a href="{{ content.link }}" class="box-product poker {% if box.poker_block_hot == true %} hot{% endif %}" style="background-image: url('{{ TimberImage(box.poker_block_logo).src }}" data-slug="{{ content.postslug }}">
                <div class="btn btn-game">เล่นเกม</div>
            </a>
            {% endfor %}
        </div>
    </div>
</section>

{% endblock %}